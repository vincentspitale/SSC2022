import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("With this app you can convert images of handwriting to vector paths. These paths can be manipulated exactly like those drawn directly on the device. For the best editing experience please use an Apple Pencil.")
        }
    }
}
